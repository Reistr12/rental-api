import { Controller } from "@nestjs/common";

@Controller('users')
export class UpdatePartialUserController {
  // This controller will handle user update requests
  // Implementation will be added later
}